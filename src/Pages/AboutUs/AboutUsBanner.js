import { Grid, makeStyles } from "@material-ui/core"
import { Item } from "./Item";


export const AboutUsBanner = ({item}) => {
    return (
    <Grid xs={12}>
        <Item item={item}>

        </Item>
    </Grid>
    );
}